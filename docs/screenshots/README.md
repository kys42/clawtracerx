# ocmOn (ClawTracerX) — Screenshots

Representative, non-sensitive UI screenshots for the README.

- `home.png` — Home overview (high-level counters + feature entry points).
- `sessions.png` — Sessions list (filtering by agent/type and recent runs).
- `cost.png` — Cost dashboard (cost/token/session breakdown charts).

> Note: additional screens (session detail: tools/subagent tree, graph, schedule/cron, settings/about) were not captured due to intermittent browser relay tab control timeouts during navigation.
